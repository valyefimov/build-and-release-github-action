{
  "name": "build-and-release-github-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p build && touch build/$(date +%s%N).txt",
    "archive": "bestzip ./build.zip ./build/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bestzip": "^2.2.1"
  }
}
